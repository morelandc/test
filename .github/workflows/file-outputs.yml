name: file-outputs

on:
  workflow_dispatch:

jobs:
  store-from-file:
  runs-on: ubuntu-latest
    outputs:
      my-num: ${{ step.step1.outputs.num1 }}
    steps:
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          repository: morelandc/java-program
      - name: Run Java file
        run: java Main.java
          
        
