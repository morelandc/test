name: file-outputs

on:
  workflow_dispatch:

jobs:
  store-from-file:
  runs-on: ubuntu-latest
    outputs:
      my-num: ${{ step.step1.outputs.num1 }}
    steps:
      -name: Checkout repo
        uses: actions/checkout@v4
      -id: store-file-output
        run: |
          
        
